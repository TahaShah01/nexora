# Shared Types (by convention, not by import)

Frontend and backend are fully independent projects, so types here are a
*reference contract*, duplicated into each app's `src/types` as they're
implemented. Kept here so both sides can be diffed against one source when
a schema changes.
